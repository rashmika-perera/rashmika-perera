name: Generate Colorful Snake Animation

on:
  schedule:
    - cron: "0 */12 * * *" # runs every 12 hours
  workflow_dispatch: # manual trigger

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Checkout the repo
      - name: Checkout
        uses: actions/checkout@v4

      # Generate colorful snake SVG
      - name: Generate Colorful Snake
        uses: Platane/snk@v3
        with:
          github_user_name: rashmika-perera
          outputs: |
            dist/snake-colorful.svg?color_snake=ff0000,ff7f00,ffff00,00ff00,0000ff,4b0082,8b00ff&color_grid=0d1117&color_dots=#30363d

      # Upload artifact (new version)
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: snake-colorful
          path: dist/snake-colorful.svg

      # Deploy to "output" branch
      - name: Deploy to output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
